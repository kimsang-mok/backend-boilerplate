const path = require("path");

module.exports = {
  config: path.join("dist", "configs", "index.js"),
  "models-path": path.join("dist", "configs", "sequelize.js"),
  "migrations-path": path.join("dist", "migrations")
};
